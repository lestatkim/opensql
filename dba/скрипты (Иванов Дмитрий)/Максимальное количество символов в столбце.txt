SELECT MAX(LEN(field_name)) FROM table_name;

--Этот запрос выдаст максимальное количество символов, которое присутствует на данный момент в столбце field_name. 
--Если значение не превышает 8000, то можно смело поменять тип данных столбца на varchar(8000)